// Declare the package for the interface
package com.baeldung.decorator;

// Declare the interface "Pizza"
public interface Pizza {
    
    // Declare the method signature for obtaining pizza toppings
    String Toppings();
}
